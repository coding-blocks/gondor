#import "../../fragments/eventInvite.graphql"

mutation CreateEvent($input: CalendarEventInviteInput!) {
  invites: calendarEventInvite(input: $input) {
    ...EventInviteFragment
    user {
      id
    }
  }
}
