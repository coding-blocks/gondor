#import "../../fragments/event.graphql"
#import "../../fragments/resource.graphql"

mutation CreateEvent($input: CalendarEventCreateInput!) {
  calendarEventCreate(input: $input) {
    ...EventFragment
    resources {
      ...ResourceFragment
    }
  }
}
