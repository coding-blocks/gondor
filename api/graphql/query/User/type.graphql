enum UserRoleName {
  Admin
  Mentor
  Member
}

type User @define(entity: "user") {
  id: ID!
  username: String!
  roles: [UserRoleName!]!
  firstname: String
  lastname: String
  email: String @authorize
  mobile_number: String @authorize
  photo: String @authorize
}
